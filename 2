var x, y: real;
begin
x := -11;
while x <= 1 do
  begin
  if (x<-9) then 
    y:=x*(power(e,x))
    else if x<-1  then
       y:= (power(x,3))/60
       else y:= (power(x,0.1*x))-tan(x);
       writeln('f (', x:0:2, ') = ', y:0:2);
       x:=x+ 0.1;
    end;
end.
